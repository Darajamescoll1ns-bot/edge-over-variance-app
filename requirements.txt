# Web layer only — the scoring engine itself is standard-library and needs none of this.
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9   # required by FastAPI for HTML form posts
httpx==0.27.2             # used by the TestClient in test_app.py
